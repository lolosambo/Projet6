{% extends 'base.html.twig' %}

{% block title %}SNOWTRICKS LOGIN{% endblock %}
{% block header %}
    <div class="container">
        <div class="row">
            <div class="col s12 white-text">
                <h2><i class="medium material-icons prefix">group_add</i> Réinitialisation du mot de passe</h2>
            </div>
        </div>
        <div class="row">
            {{ form_start(form) }}
            <div class="input-field col s12">
                <i class="material-icons prefix white-text">vpn_key</i>
                <label class="active">{{ form_label(form.password.first) }}</label>
                {{ form_widget(form.password.first) }}
                {% if form_errors(form.password.first) %}
                    <div class="form-errors">{{ form_errors(form.password.first) }}</div>
                {% endif %}
            </div>
            <div class="input-field col s12">
                <i class="material-icons prefix white-text">vpn_key</i>
                <label class="active">{{ form_label(form.password.second) }}</label>
                {{ form_widget(form.password.second) }}
                {% if form_errors(form.password.second) %}
                    <div class="form-errors">{{ form_errors(form.password.second) }}</div>
                {% endif %}
            </div>
            <div class="col s12 center-align">
                <button type="submit" name="confirm" class="waves-effect waves-light btn-small green darken-1">Confirmer</button>
                <button type="submit" name="cancel" class="waves-effect waves-light btn-small pink darken-1">Effacer</button><br><br>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}